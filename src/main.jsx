import React from 'react'
import ReactDOM from 'react-dom/client'

import HandballFixtureApp from './HanballFixtureApp'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <HandballFixtureApp />
  </React.StrictMode>,
)
